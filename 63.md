How can we keep the workflows code DRY ?
    The options "Reusable Workflows" 

        Declare the workflow once in a separte repository and source it to the needed workflows as needed.
        
    GitHub reusable workflows let you run entire pipeline jobs from other workflows using the workflow_call trigger and the uses keyword. They prevent code copy-pasting, accept custom inputs, and securely inherit secrets. For official syntax and patterns