Lets start the roboshop servers and the expectation is app should work 100%
As we are also using the DB, it should allow Mike to login and we should also be able to able to see the orders that he has placed.
> We are able to see that the application is working and the same is expected from all during the practice.

Todays Agenda:
1) Identify and document the problems ( Record them on project board on github )
2) Creating a GitHub Account
3) Downloading Gitbash for windows users ( For Mac & Linux users, no need , rather for mac, run $ brew install git and for linux computers, run $ dnf install git)
4) Download and Install VSCode ( This is an editor where we write the code and we automate the process )
Using IDE (Integrated Development Environment), we don't have to spend much time on syntaxes.

I want everyone to have GitHub Account. We will develop and manage our automation on this GitHub.
This is going to act like a Portfolio !!!!

Typically before we do any changes on prod, we test it on multiple environments
    
    Dev ---> QA ---> Stage ---> Prod 

We will adopt, 

    Dev ---> Prod

Install GitBash on windows computer, this is Local Git ( Client GIT ) : This also offers us to a local terminal.

$ git push  ( Pushes the changes to remote github )
$ git pull  ( Pulls the changes to local )
$ git clone ( Download the entire repository )

1) Steps to push the code to github:
    Create github account
    Create an ORG
    Create a repo in the org
    Clone the repo 


Make sure to nstall the VSCode for windows

To push changes, be in the repo drectoru and run
$ git add . ; git commit -m "Testing" ; git push